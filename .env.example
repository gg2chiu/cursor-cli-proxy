# Cursor CLI Relay Configuration

# Path to the cursor-agent binary
# Default: assumes 'cursor-agent' is in system PATH
CURSOR_BIN=cursor-agent

# Default Cursor API Key
# If set, this will be used when no Authorization header is provided
CURSOR_KEY=sk-xxxx-your-default-key

# Service listening address
# Default: 0.0.0.0
HOST=0.0.0.0

# Service listening port
# Default: 8000
PORT=8000

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# Default: INFO
LOG_LEVEL=INFO

# Base directory for cursor-relay workspaces and sessions
# Default: /tmp/.cursor-relay
CURSOR_RELAY_BASE=/tmp/.cursor-relay
