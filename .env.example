# Cursor CLI Proxy Configuration
# Copy this file to .env and adjust values as needed

# Path to cursor-agent executable
CURSOR_BIN=cursor-agent

# Default Cursor API key (optional, can also be passed via Authorization header)
CURSOR_KEY=

# Base directory for session workspaces
CURSOR_RELAY_BASE=/tmp/.cursor-relay

# Server configuration
HOST=0.0.0.0
PORT=8000

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Workspace whitelist (comma-separated list of allowed absolute paths)
# Clients can specify workspace in system prompt using <workspace>/path</workspace> tag
# Only paths in this whitelist (or subdirectories) will be accepted
# Example: WORKSPACE_WHITELIST=/home/user/projects,/opt/workspace
WORKSPACE_WHITELIST=
