# Cursor CLI Proxy Docker Configuration
# Copy this file to .env and adjust values as needed

# Default Cursor API key (optional, can also be passed via Authorization header)
CURSOR_KEY=

# Server configuration
HOST=0.0.0.0
PORT=8000

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Enable info in <think> block for first response (outputs session_id and slash_commands)
# Set to true to enable, false to disable (default: false)
ENABLE_INFO_IN_THINK=false

# Workspace whitelist
# Clients can specify workspace in system prompt using <workspace>/path</workspace> tag
# Only paths in this whitelist (or subdirectories) will be accepted
# Example: WORKSPACE_WHITELIST_1=/home/user/projects
WORKSPACE_WHITELIST_1=
WORKSPACE_WHITELIST_2=
WORKSPACE_WHITELIST_3=
WORKSPACE_WHITELIST_4=
WORKSPACE_WHITELIST_5=
